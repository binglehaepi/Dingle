# 🔄 자동 업데이트 배포 가이드

**Digital Scrap Diary - Electron 자동 업데이트**

이 가이드는 electron-updater를 사용하여 자동 업데이트 기능을 구현하고 배포하는 방법을 설명합니다.

---

## 📋 목차

1. [개요](#개요)
2. [설정 확인](#설정-확인)
3. [GitHub 저장소 설정](#github-저장소-설정)
4. [배포 프로세스](#배포-프로세스)
5. [사용자 경험](#사용자-경험)
6. [트러블슈팅](#트러블슈팅)

---

## 개요

### 자동 업데이트 작동 방식

```mermaid
sequenceDiagram
    participant User as 사용자
    participant App as Digital Scrap Diary
    participant GitHub as GitHub Releases
    
    User->>App: 앱 실행
    App->>GitHub: 새 버전 확인 (latest.yml)
    
    alt 새 버전 있음
        GitHub-->>App: 버전 1.0.1 정보
        App->>User: 알림 표시
        User->>App: "다운로드" 클릭
        App->>GitHub: 설치 파일 다운로드
        GitHub-->>App: .exe/.dmg 파일
        App->>User: "다운로드 완료. 지금 재시작?"
        User->>App: "지금 재시작" 클릭
        App->>App: 자동 설치 및 재시작
        App->>User: 새 버전으로 실행
    else 최신 버전
        GitHub-->>App: 버전 1.0.0 (최신)
        App->>User: 정상 실행
    end
```

### 구현된 기능

- ✅ **자동 업데이트 확인**: 앱 시작 5초 후 자동으로 새 버전 확인
- ✅ **백그라운드 다운로드**: 사용자가 앱을 사용하는 동안 백그라운드에서 다운로드
- ✅ **진행률 표시**: 다운로드 진행률을 실시간으로 표시
- ✅ **사용자 알림**: 새 버전이 준비되면 알림 표시
- ✅ **원클릭 설치**: "지금 재시작" 버튼으로 즉시 업데이트
- ✅ **델타 업데이트 (Windows)**: 변경된 부분만 다운로드하여 데이터 절약

---

## 설정 확인

### 1. 패키지 설치 확인

`package.json`에 다음 패키지가 있는지 확인:

```json
{
  "dependencies": {
    "electron-updater": "^6.7.3"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  }
}
```

### 2. electron-builder.json 설정

```json
{
  "appId": "com.digitalscrapdiary.app",
  "publish": [
    {
      "provider": "github",
      "owner": "yourusername",        // ⚠️ 실제 GitHub 사용자명으로 변경
      "repo": "digitalscrapdiary",    // ⚠️ 실제 저장소명으로 변경
      "releaseType": "release"
    }
  ]
}
```

### 3. package.json 메타데이터

```json
{
  "name": "digitalscrapdiary",
  "version": "1.0.0",                // ⚠️ 버전 관리 중요!
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/digitalscrapdiary.git"  // ⚠️ 변경 필수
  }
}
```

---

## GitHub 저장소 설정

### 1. 저장소 생성

1. GitHub에서 새 저장소 생성:
   - 저장소명: `digitalscrapdiary` (또는 원하는 이름)
   - Public 또는 Private (자동 업데이트는 둘 다 작동)

2. 로컬 코드를 GitHub에 푸시:

```bash
cd c:\work\digitalscrapdiary-2

# Git 초기화 (아직 안 했다면)
git init

# 원격 저장소 추가
git remote add origin https://github.com/yourusername/digitalscrapdiary.git

# 브랜치 생성 및 코드 푸시
git branch -M main
git add .
git commit -m "feat: Initial commit with auto-update support"
git push -u origin main
```

### 2. package.json 및 electron-builder.json 수정

실제 GitHub 정보로 수정:

**package.json:**
```json
{
  "repository": {
    "url": "https://github.com/실제사용자명/digitalscrapdiary.git"
  }
}
```

**electron-builder.json:**
```json
{
  "publish": [
    {
      "provider": "github",
      "owner": "실제사용자명",
      "repo": "digitalscrapdiary"
    }
  ]
}
```

수정 후 다시 커밋:

```bash
git add package.json electron-builder.json
git commit -m "chore: Update GitHub repository info"
git push
```

---

## 배포 프로세스

### 첫 릴리스 (v1.0.0)

1. **버전 확인**

```bash
# 현재 버전 확인
grep '"version"' package.json
```

2. **Git 태그 생성 및 푸시**

```bash
# 태그 생성
git tag v1.0.0

# 태그 푸시
git push origin v1.0.0
```

3. **GitHub Actions 확인**

- GitHub 저장소 → **Actions** 탭
- "Release Desktop App" 워크플로우 실행 확인
- 약 10-20분 소요 (3개 플랫폼 빌드)

4. **릴리스 확인**

- GitHub 저장소 → **Releases** 탭
- v1.0.0 릴리스가 생성되었는지 확인
- 다음 파일들이 업로드되었는지 확인:
  - Windows: `Digital Scrap Diary Setup 1.0.0.exe`, `latest.yml`
  - macOS: `Digital Scrap Diary-1.0.0-arm64.dmg`, `latest-mac.yml`
  - Linux: `Digital-Scrap-Diary-1.0.0.AppImage`

### 업데이트 릴리스 (v1.0.1, v1.0.2, ...)

1. **코드 변경**

```bash
# 기능 추가/버그 수정
git add .
git commit -m "fix: Bug fix or feature addition"
git push
```

2. **버전 업데이트**

```bash
# 패치 버전 증가 (1.0.0 → 1.0.1)
npm version patch

# 또는 마이너 버전 증가 (1.0.0 → 1.1.0)
npm version minor

# 또는 메이저 버전 증가 (1.0.0 → 2.0.0)
npm version major
```

`npm version` 명령은 자동으로:
- `package.json`의 버전 업데이트
- Git 태그 생성 (v1.0.1)
- Git 커밋 생성

3. **푸시**

```bash
# 코드와 태그를 함께 푸시
git push --follow-tags
```

4. **GitHub Actions 자동 빌드**

- GitHub Actions가 자동으로 빌드 시작
- 약 10-20분 후 새 릴리스 생성

5. **사용자 업데이트**

- 사용자가 앱을 실행하면 자동으로 새 버전 감지
- 알림 표시 → 다운로드 → 재시작 → 업데이트 완료

---

## 사용자 경험

### 업데이트 알림

사용자가 앱을 실행하면 다음과 같은 알림이 표시됩니다:

1. **새 버전 발견**
   ```
   🎉 새 버전 1.0.1이 있습니다!
   
   [다운로드]  [나중에]
   ```

2. **다운로드 중**
   ```
   ⬇️ 업데이트 다운로드 중...
   
   ████████░░░░░░░░░░ 45.2%
   (27.3 MB / 60.5 MB)
   ```

3. **다운로드 완료**
   ```
   ✅ 업데이트 준비 완료!
   
   버전 1.0.1이 다운로드되었습니다.
   지금 재시작하시겠습니까?
   
   [지금 재시작]  [나중에]
   ```

4. **재시작 후**
   - 자동으로 설치
   - 새 버전으로 실행

---

## 트러블슈팅

### 문제 1: "업데이트 확인 실패"

**원인:**
- GitHub 저장소 정보가 올바르지 않음
- 네트워크 연결 문제
- GitHub Releases가 생성되지 않음

**해결:**
1. `package.json`과 `electron-builder.json`의 GitHub 정보 확인
2. GitHub → Releases 탭에서 릴리스가 있는지 확인
3. 개발자 도구(F12) → Console에서 에러 메시지 확인

### 문제 2: "다운로드가 시작되지 않음"

**원인:**
- `latest.yml` 또는 `latest-mac.yml` 파일이 없음
- 설치 파일이 업로드되지 않음

**해결:**
1. GitHub Releases에서 다음 파일 확인:
   - Windows: `latest.yml`
   - macOS: `latest-mac.yml`
2. GitHub Actions 로그에서 빌드 에러 확인

### 문제 3: "개발 모드에서 업데이트 확인"

**설명:**
- 개발 모드(`npm run dev`)에서는 자동 업데이트가 비활성화됩니다.
- 이는 의도된 동작입니다.

**테스트 방법:**
1. 프로덕션 빌드 생성:
   ```bash
   npm run electron:build:win
   ```
2. `release/` 폴더의 설치 파일 실행
3. 설치 후 앱 실행하여 업데이트 확인

### 문제 4: "버전이 증가했는데 업데이트가 감지되지 않음"

**원인:**
- 시맨틱 버저닝 규칙을 따르지 않음
- 태그를 푸시하지 않음

**해결:**
1. 버전 형식 확인: `1.0.0`, `1.0.1`, `1.1.0` (올바름)
2. `v` 접두사 확인: `v1.0.0` (올바름), `1.0.0` (태그에는 v 필요)
3. 태그가 푸시되었는지 확인:
   ```bash
   git tag
   git push origin v1.0.1
   ```

### 문제 5: "Windows SmartScreen 경고"

**설명:**
- 코드 서명이 없는 앱은 Windows SmartScreen 경고가 표시됩니다.
- 자동 업데이트는 정상 작동하지만 사용자 경험이 나쁠 수 있습니다.

**해결 (선택사항):**
1. Code Signing Certificate 구매 ($80-300/년)
2. `electron-builder.json`에 인증서 설정
3. 상세 내용은 `CODE_SIGNING_GUIDE.md` 참조

---

## 로컬 테스트

### 개발 모드 테스트

개발 모드에서는 자동 업데이트가 비활성화됩니다. 테스트하려면:

1. **v1.0.0 빌드 및 설치**

```bash
npm run electron:build:win
cd release
# Digital Scrap Diary Setup 1.0.0.exe 실행하여 설치
```

2. **코드 수정 및 v1.0.1 릴리스**

```bash
# 코드 수정
git add .
git commit -m "test: Update test"

# 버전 업
npm version patch

# 빌드 (GitHub에 푸시하지 않고 로컬 테스트)
npm run electron:build:win
```

3. **GitHub에 v1.0.1 릴리스 생성**

- 수동으로 GitHub Releases에 v1.0.1 생성
- `release/` 폴더의 파일들을 업로드

4. **설치된 v1.0.0 앱 실행**

- 자동으로 v1.0.1 감지
- 업데이트 다운로드 및 설치 테스트

---

## v1.0.3 → v1.0.4 자동 업데이트 테스트 (2026-01-30)

### 현재 상황

- **v1.0.3 릴리스**: GitHub Actions로 빌드 중
- **package.json 버전**: 1.0.3 (방금 업데이트됨)
- **목적**: 실제 사용자 환경에서 자동 업데이트 기능 검증

### 왜 자동 업데이트가 "작동하지 않는 것처럼" 보이는가?

v1.0.3이 현재 **최신 버전**이므로, 앱을 실행해도 "업데이트할 것"이 없습니다. 자동 업데이트를 테스트하려면 **더 높은 버전(v1.0.4)** 을 릴리스해야 합니다.

```
[사용자 PC]                [GitHub Release]
v1.0.3 설치         →      v1.0.3 (최신)
앱 실행 → 체크       →      "업데이트 없음" ❌
---------------------------------------------
개발자: v1.0.4 릴리스  →    v1.0.4 (최신)
---------------------------------------------
v1.0.3 앱 재실행     →      v1.0.4 감지! ✅
업데이트 알림 표시   ←      다운로드 가능
```

### 자동 업데이트 작동 원리

앱 시작 후 5초 뒤, 다음 로직이 자동 실행됩니다 ([electron/main.ts:932-937](electron/main.ts)):

```typescript
if (app.isPackaged) {
  setTimeout(() => {
    autoUpdater.checkForUpdates().catch((err) => {
      console.error('[updater] Auto-check failed:', err);
    });
  }, 5000);
}
```

업데이트 체크 과정:
1. GitHub Release의 `latest.yml` 파일 다운로드
2. 파일에 명시된 최신 버전과 현재 앱 버전 비교
3. 최신 버전이 더 높으면 → 업데이트 알림 표시
4. 같거나 낮으면 → 조용히 계속 실행

### 단계별 테스트 절차

#### 1단계: v1.0.3 빌드 완료 대기

```bash
# GitHub Actions 빌드 상태 확인
# https://github.com/binglehaepi/Dingle/actions
```

빌드가 완료되면 다음 파일들이 생성됩니다:
- Windows: `Dingle Setup 1.0.3.exe`, `latest.yml`
- Linux: `Dingle-1.0.3.AppImage`

#### 2단계: Windows에서 v1.0.3 설치

1. GitHub Releases에서 `Dingle Setup 1.0.3.exe` 다운로드
2. 설치 실행 (기존 버전이 있다면 덮어쓰기)
3. 앱 실행

#### 3단계: 업데이트 체크 로그 확인

**개발자 도구 열기:**
- Windows: `Ctrl + Shift + I`
- macOS: `Cmd + Option + I`

**Console 탭에서 확인:**
```
[updater] Checking for updates...
[updater] Update not available. Current version: 1.0.3
```

현재는 v1.0.3이 최신이므로 "업데이트 없음"이 정상입니다.

#### 4단계: 테스트용 v1.0.4 릴리스 생성

```bash
# 코드에 작은 변경 추가 (테스트 식별용)
echo "// Test update for v1.0.4" >> electron/main.ts

# 변경사항 커밋
git add .
git commit -m "test: v1.0.4 auto-update test"

# package.json 버전을 1.0.4로 업데이트
npm version patch --no-git-tag-version

# 수동으로 버전 변경 커밋
git add package.json
git commit -m "chore: bump version to 1.0.4"

# 태그 생성 및 푸시
git tag v1.0.4
git push origin main
git push origin v1.0.4
```

#### 5단계: GitHub Actions 빌드 완료 대기

- [GitHub Actions](https://github.com/binglehaepi/Dingle/actions)에서 v1.0.4 빌드 진행 상황 확인
- 약 5-10분 소요
- Releases에 v1.0.4가 생성되었는지 확인

#### 6단계: v1.0.3 앱에서 자동 업데이트 테스트

1. **v1.0.3 앱을 재시작** (또는 5분 이상 실행 중이었다면 그대로)
2. **개발자 도구(F12) Console 확인:**
   ```
   [updater] Checking for updates...
   [updater] Update available: 1.0.4
   ```

3. **화면에 업데이트 알림 표시:**
   ```
   🎉 새 버전 1.0.4가 있습니다!
   
   [다운로드]  [나중에]
   ```

4. **"다운로드" 버튼 클릭**
   ```
   [updater] Download progress: 0.00%
   [updater] Download progress: 15.32%
   [updater] Download progress: 45.78%
   ...
   [updater] Update downloaded: 1.0.4
   ```

5. **다운로드 완료 알림:**
   ```
   ✅ 업데이트 준비 완료!
   
   버전 1.0.4가 다운로드되었습니다.
   지금 재시작하시겠습니까?
   
   [지금 재시작]  [나중에]
   ```

6. **"지금 재시작" 클릭**
   - 앱이 자동으로 종료
   - 백그라운드에서 v1.0.4 설치
   - 새 버전으로 자동 재시작

7. **버전 확인:**
   - 앱 정보 또는 About 화면에서 "1.0.4" 확인
   - 또는 개발자 도구에서:
     ```javascript
     console.log(require('electron').app.getVersion())
     // 출력: "1.0.4"
     ```

### 예상되는 로그 출력

**v1.0.3 앱 실행 (v1.0.4 릴리스 전):**
```
[updater] Checking for updates...
[updater] Update not available. Current version: 1.0.3
```

**v1.0.3 앱 실행 (v1.0.4 릴리스 후):**
```
[updater] Checking for updates...
[updater] Update available: 1.0.4
// 사용자가 "다운로드" 클릭
[updater] Download progress: 0.00%
[updater] Download progress: 23.45%
[updater] Download progress: 56.78%
[updater] Download progress: 89.12%
[updater] Download progress: 100.00%
[updater] Update downloaded: 1.0.4
// 사용자가 "지금 재시작" 클릭
// 앱 종료 → 설치 → 재시작
```

**v1.0.4 앱 재시작 후:**
```
[updater] Checking for updates...
[updater] Update not available. Current version: 1.0.4
```

### 트러블슈팅

**Q: v1.0.4를 릴리스했는데 v1.0.3 앱에서 감지가 안 돼요**

A: 다음을 확인하세요:
1. GitHub Releases에 v1.0.4가 정상적으로 생성되었는지
2. `latest.yml` 파일이 업로드되었는지
3. `latest.yml` 파일 내용에 `version: 1.0.4`가 있는지
4. v1.0.3 앱을 완전히 재시작했는지 (5초 대기 후 자동 체크)

**Q: 다운로드가 너무 느려요**

A: Windows에서는 "델타 업데이트"를 지원하여 변경된 부분만 다운로드합니다. 하지만 첫 릴리스 간 차이가 크면 전체 파일을 다운로드해야 합니다.

**Q: "지금 재시작"을 눌렀는데 앱이 다시 시작되지 않아요**

A: 이는 정상입니다. 업데이트 설치는 백그라운드에서 진행되며, 수동으로 다시 앱을 실행하면 새 버전으로 시작됩니다.

### 테스트 완료 체크리스트

- [ ] v1.0.3 빌드 완료 확인
- [ ] v1.0.3 설치 및 실행
- [ ] 개발자 도구에서 `[updater]` 로그 확인
- [ ] "Update not available" 메시지 확인 (정상)
- [ ] v1.0.4 릴리스 생성
- [ ] v1.0.3 앱 재실행
- [ ] "Update available: 1.0.4" 로그 확인
- [ ] 업데이트 알림 UI 표시 확인
- [ ] 다운로드 진행률 표시 확인
- [ ] "업데이트 준비 완료" 알림 확인
- [ ] 재시작 후 v1.0.4로 업데이트 완료 확인

---

## 참고 자료

### 공식 문서

- [electron-updater 문서](https://www.electron.build/auto-update)
- [electron-builder 문서](https://www.electron.build/)
- [GitHub Releases 가이드](https://docs.github.com/en/repositories/releasing-projects-on-github)

### 프로젝트 문서

- `DEPLOYMENT_GUIDE.md` - 일반 배포 가이드
- `CODE_SIGNING_GUIDE.md` - 코드 서명 가이드
- `FINAL_DEPLOYMENT_SUMMARY.md` - 배포 요약
- `.github/workflows/release.yml` - GitHub Actions 워크플로우

### 관련 파일

- `electron/main.ts` - 자동 업데이트 로직
- `electron/preload.ts` - IPC 채널 정의
- `components/UpdateNotification.tsx` - 업데이트 알림 UI
- `electron-builder.json` - 빌드 설정

---

## 요약

### 배포 체크리스트

- [ ] GitHub 저장소 생성
- [ ] `package.json`의 `repository.url` 수정
- [ ] `electron-builder.json`의 `owner`, `repo` 수정
- [ ] 코드를 GitHub에 푸시
- [ ] 첫 태그 생성 및 푸시 (`v1.0.0`)
- [ ] GitHub Actions 빌드 확인
- [ ] Releases에서 파일 확인 (`latest.yml`, `.exe`, `.dmg` 등)
- [ ] 로컬에서 설치 및 업데이트 테스트

### 업데이트 체크리스트

- [ ] 코드 변경 및 커밋
- [ ] `npm version patch/minor/major`
- [ ] `git push --follow-tags`
- [ ] GitHub Actions 자동 빌드 대기
- [ ] 사용자 앱에서 업데이트 확인 테스트

---

## 지원

문제가 발생하면:

1. GitHub Issues에 버그 리포트
2. GitHub Discussions에서 질문
3. 프로젝트 문서 참조

---

**작성일**: 2026-01-30  
**프로젝트**: Digital Scrap Diary v1.0.0  
**문서 버전**: 1.0

